/** See `app/admin-app/layout.tsx` — the gate lives in `(protected)/layout.tsx`. */
export const metadata = { title: 'Superadmin — Tickethalo' }

export default function SuperadminAppLayout({ children }: { children: React.ReactNode }) {
  return children
}
